# Run the Application

Use this skill when the user wants to run the Cool Chord application.

## Steps
1. Ensure virtual environment is active: `source .venv/Scripts/activate`
2. Run: `python src/main.py`
3. The application window should open with the piano roll, keyboard,
   synth panel, drum panel, and chord panel.
